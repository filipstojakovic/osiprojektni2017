#ifndef ADMIN_H
#define ADMIN_H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int provera_slova2(char );
int provera_slova22(char );
void adminMenu();
char ponovo_meni(char );

#endif // ADMIN_H
