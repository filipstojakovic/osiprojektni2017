#ifndef SIGNIN_H
#define SIGNIN_H

#include <stdio.h>
#include <string.h>

int checkString(char *);//checks if the entered word is in correct form
int checkPin(char *);//checks if the entered pin is in correct form
void signin();//function that makes a new account for administrator or analyst

#endif // SIGNIN_H
